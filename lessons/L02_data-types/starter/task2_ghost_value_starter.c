/*
 * 任务 2 起步模板：捕捉“幽灵垃圾值”与安全赋初值
 * 对应课件: 第02讲 PPT 第20页【实操工坊·探究二：捕捉幽灵垃圾值】
 * 适用环境: Visual Studio 2022 / Dev-C++ / VS Code
 * 
 * 任务说明:
 * 1. 观察未赋初值的局部变量在读取时出现的离奇现象（幽灵垃圾值）
 * 2. 观察 Visual Studio 2022 编译器的 C4700 安全拦截机制
 * 3. 按照 TODO 指引为 score 赋予初值 0，消除报错与隐患，使程序健康运行
 */

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main(void)
{
    printf("====================================================\n");
    printf("     【探究二】捕捉“幽灵垃圾值”与赋初值修复实验     \n");
    printf("====================================================\n\n");

    // TODO 1: 声明一个整型变量 score，并为其赋予初值 0（规范初始化）
    // 注意：若未赋初值直接读取，在 Dev-C++ 中会输出随机负数，在 VS 中会触发 C4700 报错拦截
    int score = 0;

    printf("经过规范赋初值后，变量 score 的值为: %d\n\n", score);
    printf("【实验结论】: 声明变量的同时赋初值，消灭80%%以上的诡异Bug！\n");
    printf("====================================================\n");

    return 0;
}

# 第05讲 当堂练习与测评题单

> **适用环节**：课堂互动、多分支找茬、CPU打表追踪、黑盒测试与离堂小测  
> **对应课件**：第05讲 PPT 第6、10、13、18、19页

---

## 🎯 一、 【找茬大作战】多分支语法与穿透排雷（对应 PPT 第6页）

请指出下列 3 个代码选段中存在的语法错误或执行异常现象：

### 选段 A（浮点悲剧）
```c
float score = 85.5f;
switch (score) 
{
    case 85.5: printf("优秀\n"); break; 
}
```
- 异常原因：______

### 选段 B（变量越权）
```c
int x = 10, y = 10;
switch (x)
{
    case y: printf("x 等于 y\n"); break;
}
```
- 异常原因：______

### 选段 C（漏写 break 连锁反应）
```c
int option = 1;
switch (option)
{
    case 1: printf("洗洗手 ");
    case 2: printf("准备吃饭 ");
    case 3: printf("收起碗筷\n"); break;
}
```
- 控制台最终输出：______

---

## 🧠 二、 【内存打表跟踪】循环执行时序预测（对应 PPT 第10、13页）

### 题目 1：while 循环执行分析
已知代码片段：
```c
int i = 1, sum = 0;
while (i <= 4)
{
    sum = sum + i;
    i++;
}
```
请在下表中写出每一轮循环后的内存变量值：

| 循环轮次 | 判定 `i <= 4` | 执行 `sum = sum + i` 后的 `sum` | 执行 `i++` 后的 `i` |
|:---:|:---:|:---:|:---:|
| 第 1 轮 | 真 | 1 | 2 |
| 第 2 轮 | 真 | ______ | ______ |
| 第 3 轮 | 真 | ______ | ______ |
| 第 4 轮 | 真 | ______ | ______ |
| 跳出循环时 | **假** | 最终 `sum = ` ______ | 最终 `i = ` ______ |

### 题目 2：do-while 边界推演
```c
int k = 5;
do {
    k--;
} while (k > 0);
```
- 循环体总共执行了 ______ 次，循环结束后 `k` 的最终值是 ______。

---

## 👥 三、 【同伴互评】智能计算器与阶乘黑盒破坏性测试（对应 PPT 第18页）

与同伴交换电脑，测试其程序的健壮性：

| 测试项目 | 输入测试数据 | 理论预期结果 | 同伴程序实际表现 | 评价 |
|:---|:---|:---|:---|:---:|
| **测试1：菜单越界** | 输入菜单选项 `5` | 提示“无效选择”，再次弹出主菜单 | | □ 通过 / □ 崩溃 |
| **测试2：除零防御** | 选除法 `4`，输入 `10.0 0` | 报错“除数不能为0”，拦截闪退 | | □ 成功拦截 / □ 闪退 |
| **测试3：阶乘累加** | 计算 $1!$ 到 $5!$ 的累加和 | 精确输出 `153` (无溢出) | | □ 精确 / □ 溢出 |

---

## 📝 四、 离堂小测（对应 PPT 第19页，堂堂清）

### Q1. switch 判别表达式限制
下列哪种数据类型绝对**不能**放在 `switch(x)` 括号里做判别？
- **A.** `int`
- **B.** `char`
- **C.** `double`
- **D.** `short`

### Q2. 循环核心特性辨析
`while` 与 `do-while` 循环最核心的物理区别是什么？

### Q3. 循环控制三大基石
写出任何一个合格循环中绝对不可缺少的“三大基石”要素。

/*
 * 练习题：switch 多分支与循环结构经典排雷改错实战
 * 对应课件: 第05讲 PPT 第16页【循环与分支结构的“常见错误”大盘点】
 * 
 * 题目要求:
 * 观察并修复下方代码中的 3 处经典逻辑与死循环漏洞，实现 1 到 10 的累加求和：
 * 漏洞 1: switch 菜单分支漏写 break 导致发生 case 穿透
 * 漏洞 2: while 循环后误加分号 ';' 导致空语句死循环
 * 漏洞 3: 循环体内部漏写循环控制变量更新 (i++) 导致死循环
 */

#include <stdio.h>

int main(void)
{
    int i = 1;
    int sum = 0;

    printf("=== 计算 1 到 10 累加和程序 ===\n");

    // 修复点: 
    // 1. while() 后面严禁加分号 ';'
    // 2. 循环体内必须有 i++ 步长更新，逼近终点
    while (i <= 10)
    {
        sum = sum + i;
        i++;  // 必须更新变量，防止死循环
    }

    printf("1 到 10 的累加和为: %d (理论应为 55)\n", sum);
    return 0;
}

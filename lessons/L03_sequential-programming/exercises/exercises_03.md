# 第03讲 当堂练习与测评题单

> **适用环节**：课堂互动、算术速算、逻辑短路推演、同伴互评与离堂小测  
> **对应课件**：第03讲 PPT 第4、6、8、13、27、28页

---

## 🎯 一、 【天条速算】除法与取模判断（对应 PPT 第4页）

请根据 C 语言“除法截断天条”与“取模符号天条”，快速口算下列表达式的结果：

1. `5 / 2` 的结果是：______
2. `5.0 / 2` 的结果是：______
3. `5 % 3` 的结果是：______
4. `-5 % 3` 的结果是：______
5. `5 % -3` 的结果是：______
6. `-5 % -3` 的结果是：______
7. `12.3 % 3` 会出现什么现象？______

---

## 🧠 二、 【内存推演】自增自减、逻辑短路与复合赋值（对应 PPT 第6、8、13页）

### 题目 1：自增自减内存动作
已知 `int i = 3, j;`：
- 执行 `j = ++i;` 后，`i = ` ______，`j = ` ______。
- 执行 `j = i++;` 后，`i = ` ______，`j = ` ______。

### 题目 2：短路求值电路模拟
已知 `int a = 0, b = 1, c;`：
执行表达式 `c = a++ && (b = 3);` 后：
- 变量 `a` 的值是：______
- 变量 `b` 的值是：______（思考：`b = 3` 执行了吗？为什么？）
- 变量 `c` 的值是：______

### 题目 3：复合赋值隐形括号地雷
若有 `int x = 10, y = 2;`，执行语句 `x %= y + 3;` 后，`x` 的值是多少？
- 提示：它等价于 `x = x % y + 3` 还是 `x = x % (y + 3)`？

---

## 👥 三、 【同伴互评】大满贯程序黑盒极限测试（对应 PPT 第27页）

完成上机任务后，与邻座同伴交换键盘，在其电脑上输入以下极限测试用例并记录表现：

| 被测程序文件名 | 输入测试数据 | 预期正确结果 | 同伴程序真实表现 | 测试结论 |
| :---: | :---: | :--- | :--- | :---: |
| `task3_reverse.c` | `900` | 数码总和:`9`, 逆序拼接:`9` (即整数 `9`) | | □ Pass / □ Bug |
| `task5_triangle.c`| `5 12 13` | 三角形面积: `30.00` | | □ Pass / □ Bug |

---

## 📝 四、 离堂小测（对应 PPT 第28页·闭卷独立完成）

### Q1. 自增后置求值
已知 `int i = 5, y;`，求执行完表达式 `y = i++ + 3;` 后，`y` 和 `i` 的值分别是：
- **A.** `y = 8`, `i = 5`
- **B.** `y = 8`, `i = 6`
- **C.** `y = 9`, `i = 6`

### Q2. 表达式求值（多选）
下列 C 语言表达式中，求值结果为 `2` 的选项有：
- **A.** `5 / 2`
- **B.** `5.0 / 2`
- **C.** `5 % -3`
- **D.** `-5 % 3`

### Q3. 逻辑短路分析
若有 `int a = 0, b = 2;`，执行表达式 `(a = b) || (b = 5)` 后，变量 `b` 的值是多少？请写出原理解析。

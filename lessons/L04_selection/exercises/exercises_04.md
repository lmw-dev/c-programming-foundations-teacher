# 第04讲 当堂练习与测评题单

> **适用环节**：课堂互动、优先级对决、就近配对推演、黑盒测试与离堂小测  
> **对应课件**：第04讲 PPT 第8、11、13、17、19页

---

## 🎯 一、 【优先级对决】混合表达式求值（对应 PPT 第8页）

已知 `int a = 3, b = 4, c = 5;`，请口算出下列表达式的真假值（`1` 代表真，`0` 代表假）：

1. `a + b > c && b == c` 的结果是：______
2. `a || b + c && b - c` 的结果是：______（提示：非 0 即为真，-1 是真还是假？）
3. `!(a > b) || !c` 的结果是：______
4. `a = 0 || b > 2` 执行后，表达式的值是：______，变量 `a` 的值是：______

---

## 🧠 二、 【内存与语法推演】就近配对与语法排雷（对应 PPT 第11、14页）

### 题目 1：迷失的 else
观察下方代码：
```c
int a = -5, b = -5;
if (a > 0)
    if (b > 0) printf("AAA\n");
else
    printf("BBB\n");
```
- 请问：代码中的 `else` 实际上与哪个 `if` 配对？______
- 执行这段代码后，控制台屏幕会输出什么？______

### 题目 2：找茬排雷
指出下方代码片段中存在的 1 处致命语法错误或逻辑漏洞：
```c
int score = 85;
if (score >= 60);
{
    printf("考试及格！\n");
}
```
- 漏洞分析：______

---

## 👥 三、 【同伴互评】智能计算器极限黑盒破坏性测试（对应 PPT 第17页）

与同伴交换电脑，在其终端中输入以下极限数据，测试其计算器的健壮性：

| 用例序号 | 测试输入数据 | 理论预期结果 | 同伴程序实际表现 | 测试结论 |
|:---:|:---|:---|:---|:---:|
| **用例 1** | `12.5 + 3.5` | 正常输出 `16.00` | | □ 通过 / □ 异常 |
| **用例 2 (除零测试)** | `10.5 / 0` | 友好拦截并报错“除数不能为零” | | □ 友好拦截 / □ 崩溃闪退 |
| **用例 3 (非法字符)** | `10.5 # 2.0` | 报错“未知运算符” | | □ 正常报错 / □ 计算错误 |
| **用例 4 (近零微小值)** | `5.0 / 0.00000001` | 正常计算或精密除零拦截 | | □ 通过 / □ 异常 |

---

## 📝 四、 离堂小测（对应 PPT 第19页，堂堂清）

### Q1. 嵌套配对读程序
执行下面这段代码后，输出的结果是：
```c
int x = 5, y = 10;
if (x == 5)
    if (y == 20) printf("AAA\n");
else
    printf("BBB\n");
```
- **A.** `AAA`
- **B.** `BBB`
- **C.** 什么都不输出

### Q2. 字符区间判断
要表达“字符变量 `ch` 是大写英文字母”的正确 C 语言条件表达式是：
- **A.** `'A' <= ch <= 'Z'`
- **B.** `ch >= 'A' && ch <= 'Z'`
- **C.** `ch >= 'A' || ch <= 'Z'`

### Q3. 短路求值阐述
请简述 C 语言中的“短路求值”机制，并举例说明 `a && b` 在什么情况下会发生短路。

/*
 * 练习题：选择结构与条件判断经典排雷改错实战
 * 对应课件: 第04讲 PPT 第13~14页【质检素养 & 初学者 8 大翻车地雷】
 * 
 * 题目要求:
 * 观察并修复下方代码中的 3 处经典逻辑与语法漏洞，实现正确的“成绩等级评定系统”：
 * 漏洞 1: 等于判断误写成单等号 '=' 导致赋值恒真
 * 漏洞 2: if 后误加分号 ';' 导致大括号无条件执行
 * 漏洞 3: 临界及格边界值缺少等号 (score > 60 应为 score >= 60)
 */

#include <stdio.h>

int main(void)
{
    int score = 0;

    printf("请输入学生成绩 (0~100): ");
    if (scanf("%d", &score) != 1) {
        printf("⚠️ 输入无效！\n");
        return 1;
    }

    // 防御性合法性检查
    if (score < 0 || score > 100) {
        printf("⚠️ 错误: 成绩必须在 0 到 100 之间！\n");
        return 1;
    }

    // 修复点: 
    // 1. 判断等于必须用 '=='，不能用 '='
    // 2. if () 后面严禁加分号 ';'
    // 3. 边界值及格线需包含 60 分 (score >= 60)
    if (score == 100) {
        printf("🏆 满分！表现完美！\n");
    } else if (score >= 90) {
        printf("🌟 优秀 (90~99分)\n");
    } else if (score >= 60) {
        printf("✅ 及格 (60~89分)\n");
    } else {
        printf("❌ 不及格 (0~59分)\n");
    }

    return 0;
}

/*
 * 练习题：循环嵌套与控制流经典排雷改错实战
 * 对应课件: 第06讲 PPT 第7、15页【避坑指南：循环结构下的三大夺命地雷】
 * 
 * 题目要求:
 * 观察并修复下方代码中的 3 处经典致命错误，实现打印 3 行 3 列星号矩阵：
 * 漏洞 1: for 循环头部后面误加分号 ';' 导致空循环
 * 漏洞 2: 内外层循环使用了同一个计数器变量名 'i' 导致变量冲突失控
 * 漏洞 3: 每行打印完毕后未换行
 */

#include <stdio.h>

int main(void)
{
    printf("=== 打印 3x3 字符矩阵 ===\n");

    // 修复点:
    // 1. for 头部严禁加分号 ';'
    // 2. 外层使用 i，内层使用独立的变量名 j (禁止同名打架)
    // 3. 内层结束后在外层添加换行 printf("\n")
    for (int i = 1; i <= 3; i++)
    {
        for (int j = 1; j <= 3; j++)
        {
            printf("* ");
        }
        printf("\n"); // 每行打印完毕换行
    }

    return 0;
}

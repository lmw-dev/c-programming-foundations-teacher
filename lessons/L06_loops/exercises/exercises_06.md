# 第06讲 当堂练习与测评题单

> **适用环节**：课堂互动、时序推演、break/continue 预测、同伴黑盒测试与离堂小测  
> **对应课件**：第06讲 PPT 第4、7、13、14、18、19页

---

## 🎯 一、 【时序解剖与找茬】for 循环语法排雷（对应 PPT 第4、7页）

### 题目 1：for 循环时序路径
在 `for (表达式1; 表达式2; 表达式3) { 循环体; }` 中：
- 整个循环期间只执行 1 次的是：______
- 每次进入循环体前必须评估的是：______
- 循环体执行完毕后立刻执行的是：______

### 题目 2：找茬分析
观察下方代码片段：
```c
int sum = 0;
for (int i = 1; i <= 5; i++);
{
    sum = sum + i;
}
```
- 请问该程序能否编译成功？为什么？______

---

## 🧠 二、 【控制流物理跳转】break 与 continue 大对决（对应 PPT 第13、14页）

### 题目 1：输出结果预测
已知两段代码分别运行：

```c
// 代码 A (break)
for (int i = 1; i <= 5; i++) {
    if (i == 3) break;
    printf("%d ", i);
}
printf("End\n");
```
- 代码 A 输出：______

```c
// 代码 B (continue)
for (int i = 1; i <= 5; i++) {
    if (i == 3) continue;
    printf("%d ", i);
}
printf("End\n");
```
- 代码 B 输出：______

### 题目 2：多层嵌套下的 break 权限
```c
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (j == 2) break;
        printf("i=%d,j=%d ", i, j);
    }
}
```
- 请写出该双重循环的实际输出结果：______（思考：内层的 break 能跳出外层 i 循环吗？）

---

## 👥 三、 【同伴互评】素数过滤器极限黑盒测试（对应 PPT 第18页）

与同伴交换电脑，测试其素数判定程序的健壮性：

| 输入测试值 | 理论预期输出 | 同伴程序实际表现 | 边界判定结论 | 考查的核心边界 |
|:---:|:---:|:---|:---:|:---|
| **`2`** | **是素数** | | □ 通过 / □ 异常 | 最小素数边界 |
| **`4`** | **不是素数** | | □ 通过 / □ 异常 | 最小平方数合数边界 |
| **`9`** | **不是素数** | | □ 通过 / □ 异常 | 奇合数边界 |
| **`-5`** | **不是素数** | | □ 通过 / □ 异常 | 负数非法输入拦截 |

---

## 📝 四、 离堂小测（对应 PPT 第19页，堂堂清）

### Q1. for 循环时序与作用域
关于 `for (int i = 1; i < 10; i++)`，以下哪种说法是**完全正确**的？
- **A.** 步长 `i++` 是在每次循环体执行前执行的
- **B.** 变量 `i` 在退出当前 `for` 循环后依然可以在外部被打印和使用
- **C.** 循环体语句总共执行了 9 次，第 10 次条件判定为假而退出整个循环

### Q2. continue 跳转终点
在 `while` 循环体中，如果执行了 `continue;`，程序接下来的跳转终点是？在 `for` 循环中又是跳到哪里？

### Q3. 双重循环计数推演
如下代码的最终输出结果是：
```c
int count = 0;
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        if (i == j) continue;
        count++;
    }
}
printf("count = %d\n", count);
```
- 输出结果为：______

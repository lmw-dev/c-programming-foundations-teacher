/*
 * 文件名: 1.4_error_playground.c
 * 对应课件: 第01讲 PPT 第11页【常见错误初步辨析】
 * 对应教材: 第1章 常见语法与编译错误
 * 描述: 初学者高频报错辨析游乐场（教师演示解注释观察编译器报错）
 * 适用: C语言零基础初学者 / 教师课堂纠错示范
 * 
 * 排错口诀与顺序:
 * 读提示(行号与信息) -> 定位(光标跳到对应行) -> 修改(检查符号) -> 再运行
 */

#include <stdio.h>

int main(void)
{
    printf("=== 欢迎来到C语言报错辨析游乐场 ===\n");
    printf("请在下方依次取消某一个错误注释，重新编译并观察编译器提示：\n\n");

    // ==========================================
    // 经典错误【一】：少了英文分号 ';'
    // 现象: 编译器提示 error: expected ';' before 'return' 或 C2143
    // 原因: C语言中分号是语句的法定终结符，回车换行不能代替分号
    // ==========================================
    // printf("我忘记写末尾分号了")
    // return 0;

    // ==========================================
    // 经典错误【二】：混用中文标点符号（如中文分号 ； 或中文双引号 “”）
    // 现象: 编译器提示 error: stray '\357' 或 C2018: 未知字符
    // 解决方法: 务必切换为英文输入法半角状态
    // ==========================================
    // printf("我使用了中文分号！")；

    // ==========================================
    // 经典错误【三】：主函数名字拼写错误 (main 写成 mian)
    // 现象: 编译或链接时提示 undefined reference to `main` 或 LNK2019: 无法解析的外部符号 _main
    // 原因: C语言严格区分大小写，程序入口必须精确命名为 main
    // ==========================================
    /*
    int mian(void) {
        printf("入口拼错了！\n");
        return 0;
    }
    */

    // ==========================================
    // 经典错误【四】：转义字符斜杠方向写反 (/n 写成了普通字符)
    // 现象: 编译不报错，但屏幕上原样打印出了 /n，没有实现预期换行
    // 助记: 反斜杠 '\' 才是转义符（向后倒），正斜杠 '/' 只是除号
    // ==========================================
    printf("测试换行符反转: 这是第一段/n这还会换行吗？\n");

    // ==========================================
    // 经典错误【五】：双引号不配对或跨行断裂
    // 现象: error: missing terminating '"' character
    // ==========================================
    // printf("双引号只有前半边，忘记写后半边了);

    // ==========================================
    // 经典错误【六】：大括号 {} 不配对
    // 现象: error: expected '}' at end of input
    // ==========================================

    printf("恭喜！当前所有错误均已处于注释状态，代码可正常运行。\n");

    return 0;
}

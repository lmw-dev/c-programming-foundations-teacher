/*
 * 文件名: 1.5_sum_two_numbers.c
 * 对应课件: 第01讲 PPT 第16页【C程序进阶：求两数之和（例1.2）】
 * 对应教材: 谭浩强版《C程序设计》第1章 例1.2 + 何钦铭版第2章
 * 描述: 第一个包含“变量定义、数据赋值、算术运算与格式化输出”的进阶 C 程序
 * 适用环境: Visual Studio / Dev-C++ / VS Code / GCC / Clang
 * 
 * 核心教学知识点:
 * 1. 变量定义（int a, b, sum;）—— 向内存申请整型收纳盒，“先定义，后使用”
 * 2. 赋值运算符（=）—— 将右侧的数值放入左侧的变量盒中，非数学等号
 * 3. 表达式求值（sum = a + b;）—— CPU 算术运算与结果回写
 * 4. 格式化输出占位符（%d）—— 将整型变量的值动态替换到指定位置输出
 */

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main(void)
{
    // 1. 变量声明：在内存中开辟三个整型（Integer）变量收纳盒
    int a, b, sum;

    // 2. 赋值操作：把具体数值塞进变量收纳盒中
    a = 123;
    b = 456;

    // 3. 计算并赋值：让 CPU 计算 a + b 的值，并将结果赋给变量 sum
    sum = a + b;

    // 4. 格式化输出：%d 是整型占位符，把 sum 的数值替换到 %d 处打印
    printf("sum is %d\n", sum);

    return 0;
}

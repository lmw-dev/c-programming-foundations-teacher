# 第07讲 当堂练习与测评题单

> **适用环节**：课堂互动、语法找茬、内存字节计算、打擂台时序推演、同伴代码自查与离堂小测  
> **对应课件**：第07讲 PPT 第5、6、7、10、13、17、18页

---

## 🎯 一、 【找茬与正误判断】数组定义与初始化（对应 PPT 第5、6、7页）

请判断下列关于一维数组的代码片段是否合法，并说明原因：

1. `int n; scanf("%d", &n); int arr[n];`
   - 合法性：______ ；原因：______
2. `int a[5] = {1, 2, 3, 4, 5, 6};`
   - 合法性：______ ；原因：______
3. `int a[5]; a = {1, 2, 3, 4, 5};`
   - 合法性：______ ；原因：______
4. `int a[10] = {1, 2, 3};`
   - 此时 `a[5]` 的实际物理存储值为：______

---

## 🧠 二、 【内存模型与打表跟踪】（对应 PPT 第10、13页）

### 题目 1：字符串与内存字节
定义字符数组：`char str1[] = "China";` 与 `char str2[] = {'C', 'h', 'i', 'n', 'a'};`
- `str1` 在内存中实际占用的总字节数是：______ 字节
- `str2` 在内存中实际占用的总字节数是：______ 字节
- `str1[5]` 存放的字符是：______

### 题目 2：打擂台最值跟踪
已知数组：`int arr[5] = {75, 88, 62, 95, 81};`
请完成打擂台求最高分及其下标的追踪表：

| 挑战轮次 `i` | 挑战者 `arr[i]` | 当前擂主 `max` | 是否易主 | 更新后 `max` | 更新后 `max_idx` |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 初始状态 | - | `arr[0]=75` | - | 75 | 0 |
| `i = 1` | 88 | 75 | **是** | 88 | 1 |
| `i = 2` | 62 | 88 | ______ | ______ | ______ |
| `i = 3` | 95 | 88 | ______ | ______ | ______ |
| `i = 4` | 81 | 95 | ______ | ______ | ______ |

---

## 👥 三、 【同伴互评自查表】（对应 PPT 第17页）

与同伴交换电脑，查验对方编写的 `7.2_grade_master.c` 源码：

| 检查维度 | 判定标准 | 查验结果 | 改进建议 |
|:---|:---|:---:|:---|
| **边界合宪性** | 循环条件是否严格为 `i < 10` (绝非 `i <= 10`) | □ 规范 / □ 越界 | |
| **类型对齐度** | 平均分计算是否进行了 `(float)sum / 10.0f` 精度保护 | □ 精确 / □ 精度丢失 | |
| **下标同步性** | 更新 `max_score` 时是否同步更新了 `max_index = i` | □ 完整 / □ 遗漏 | |
| **命名规范** | 变量是否使用 `snake_case` 见名知意 | □ 优雅 / □ 随意 | |

---

## 📝 四、 离堂小测（对应 PPT 第18页，堂堂清）

### Q1. 物理内存大小计算
若定义一维数组 `int a[5];`，在 32/64 位主流现代编译器中，该数组所占用的物理内存总字节数是：
- **A.** 5 个字节
- **B.** 10 个字节
- **C.** 20 个字节
- **D.** 40 个字节

### Q2. 数组定义合法性
下列对一维数组的定义或初始化，完全合法的是：
- **A.** `int n = 5; int a[n];`
- **B.** `int a[5] = {0, 1, 2, 3, 4, 5};`
- **C.** `int a[5]; a = {0, 1, 2, 3, 4};`
- **D.** `int a[] = {1, 2, 3};`

### Q3. 字符串隐式结束符
定义字符数组 `char s[10] = "China";`，则在内存中，该字符数组的第 6 个元素 `s[5]` 存放的字符是：
- **A.** `'a'`
- **B.** `'\0'`
- **C.** 空格 `' '`
- **D.** 无效垃圾值

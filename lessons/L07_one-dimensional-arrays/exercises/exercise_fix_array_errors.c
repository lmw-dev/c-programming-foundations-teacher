/*
 * 练习题：一维数组与边界控制经典排雷改错实战
 * 对应课件: 第07讲 PPT 第15页【避坑指南：数组新手的四大经典翻车点】
 * 
 * 题目要求:
 * 观察并修复下方代码中的 3 处经典逻辑与越界漏洞，实现计算 5 位学生成绩的平均分：
 * 漏洞 1: 遍历循环条件误写为 i <= 5 导致访问 arr[5] 发生致命数组越界
 * 漏洞 2: 累加器 sum 未初始化清零，包含堆栈幽灵垃圾值
 * 漏洞 3: 求平均分时整数除法截断导致丢失小数精度
 */

#include <stdio.h>

int main(void)
{
    int score[5] = {85, 92, 78, 90, 88};
    int sum = 0;  // 修复点 1: 必须初始化为 0，杜绝幽灵垃圾值

    printf("=== 5 位学生成绩统计分析 ===\n");

    // 修复点 2: 循环条件必须为 i < 5 (有效下标 0~4)，严禁写 i <= 5
    for (int i = 0; i < 5; i++)
    {
        sum += score[i];
    }

    // 修复点 3: 强制类型转换保证浮点数除法精度
    float average = (float)sum / 5.0f;

    printf("总分: %d, 平均分: %.2f (理论应为 86.60)\n", sum, average);
    return 0;
}

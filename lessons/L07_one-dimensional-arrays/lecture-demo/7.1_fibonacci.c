/*
 * 文件名: 7.1_fibonacci.c
 * 对应课件: 第07讲 PPT 第9页【经典算法一：利用数组解决 Fibonacci 数列】
 * 对应教材: 谭浩强版《C程序设计》第6章 例6.2 + 何钦铭版《C语言程序设计》第5章 例5.2
 * 适用环境: Visual Studio / Dev-C++ / VS Code
 * 
 * 核心教学目标:
 * 1. 掌握一维数组声明与物理内存连续排布模型 (Contiguous Memory)
 * 2. 掌握空间换时间的算法思想：利用数组快速随机访问 O(1) 实现数列递推 f[i] = f[i-1] + f[i-2]
 * 3. 掌握控制台工程排版：横向制表符 '\t' 配合计数器 (i + 1) % 5 == 0 实现每行 5 个规整输出
 */

#include <stdio.h>

int main(void)
{
    printf("======================================================================\n");
    printf("        经典算法实战 —— 斐波那契数列 (前20项数组递推与格式化输出)       \n");
    printf("======================================================================\n\n");

    // =========================================================================
    // 【一维数组定义与物理空间分配】
    // 语法: int fib[20];
    // 内存机制: 系统在栈区一次性划出连续的 80 个字节 (20 * 4 字节)。
    // 下标范围: fib[0] 到 fib[19]，绝对没有 fib[20]！
    // =========================================================================
    int fib[20];

    // 1. 初始化前两个基础数值（递推的种子）
    fib[0] = 1;  // 第 1 项
    fib[1] = 1;  // 第 2 项

    // =========================================================================
    // 【核心递推计算：空间换时间】
    // 数学模型: 每一个新项等于紧随其前的两个项之和
    // 物理寻址: fib[i] 底层直接通过 基地址 + i * sizeof(int) 实现 O(1) 毫秒级直接读写！
    // =========================================================================
    for (int i = 2; i < 20; i++)
    {
        fib[i] = fib[i - 1] + fib[i - 2];  // 用前两个抽屉算出当前抽屉
    }

    // =========================================================================
    // 【工程排版输出：每行 5 个数据，列对齐】
    // =========================================================================
    printf("斐波那契数列前 20 项输出如下:\n\n");

    for (int i = 0; i < 20; i++)
    {
        // 打印元素：使用 %10d 指定 10 字符宽度，右对齐
        printf("%10d\t", fib[i]);

        // 排版控制: 每输出 5 个数字插入一个回车换行符
        // (i + 1) 代表当前已输出的总个数，当为 5, 10, 15, 20 时触发换行
        if ((i + 1) % 5 == 0)
        {
            printf("\n");
        }
    }

    printf("\n======================================================================\n");
    return 0;
}
